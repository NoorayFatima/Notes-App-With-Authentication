# Base URL of the API (root — auth and notes endpoints are both mounted
# under this). Copy this file to .env and adjust if your backend runs on
# a different port.
VITE_API_BASE_URL=http://localhost:3000/api
